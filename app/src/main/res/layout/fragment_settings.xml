<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="ca.mdietr.achieved.SettingsFragment">

    <GridLayout
        android:id="@+id/grid_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="2"
        android:rowCount="2"
        android:useDefaultMargins="true">

        <TextView
            android:id="@+id/txt_settings_reminder_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_columnSpan="2"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:textStyle="bold"
            android:text="Remind me to set a daily goal:"/>

        <TextView
            android:id="@+id/txt_settings_reminder"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:text="12:00 AM"/>

        <android.support.v7.widget.SwitchCompat
            android:id="@+id/switch_settings_reminder"
            android:layout_gravity="center_vertical"
            android:textOn="ON"
            android:textOff="OFF" />

    </GridLayout>

</FrameLayout>
